import { Meta } from "@storybook/blocks";

<Meta title="Accessibility/Option Group" />

# Accessibility — sando-option-group

> Source: [`packages/components/src/components/option-group/ACCESSIBILITY.md`](https://github.com/rodrigolagodev/SandoDesignSystem/blob/master/packages/components/src/components/option-group/ACCESSIBILITY.md)

## ARIA pattern

Follows the [WAI-ARIA Group](https://www.w3.org/WAI/ARIA/apg/patterns/) pattern for grouping options within a listbox.

<table>
  <thead>
    <tr>
      <th>Attribute</th>
      <th>Value</th>
      <th>Purpose</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`role`</td>
      <td>`group`</td>
      <td>Groups related options under a common label</td>
    </tr>
    <tr>
      <td>`aria-label`</td>
      <td>Group label text</td>
      <td>Names the group for screen readers</td>
    </tr>
    <tr>
      <td>`aria-disabled`</td>
      <td>`"true"` / `"false"`</td>
      <td>Marks all options in the group as non-interactive when disabled</td>
    </tr>
  </tbody>
</table>

The options container inside the group uses `role="presentation"`. Designed to be used inside `sando-select`.

## Keyboard map

<table>
  <thead>
    <tr>
      <th>Key</th>
      <th>Behavior</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>ArrowUp / ArrowDown</td>
      <td>Navigate through options across groups (managed by parent `sando-select`)</td>
    </tr>
  </tbody>
</table>

The group heading itself is not focusable.

## Screen reader behavior

When navigating into a group: "Fruits group" (VoiceOver) before the first option. Disabled group options: announced as "dimmed" or "unavailable".

## Color contrast

Verified via `jest-axe` against the `original` flavor in light and dark modes. Disabled state uses intentional reduced contrast.

## Tested with

- jest-axe (automated, runs in `sando-option-group.a11y.test.ts`)
- VoiceOver (macOS Sequoia, Safari 18)
- NVDA (Windows 11, Firefox 130)

## Known limitations

None identified.

---

[Back to Accessibility Statement](?path=/docs/accessibility-statement--docs)
