import { Meta } from "@storybook/blocks";

<Meta title="Accessibility/Spinner" />

# Accessibility — sando-spinner

> Source: [`packages/components/src/components/spinner/ACCESSIBILITY.md`](https://github.com/rodrigolagodev/SandoDesignSystem/blob/master/packages/components/src/components/spinner/ACCESSIBILITY.md)

## ARIA pattern

Follows the [WAI-ARIA Live Region](https://www.w3.org/WAI/ARIA/apg/patterns/) pattern using `role="status"`.

<table>
  <thead>
    <tr>
      <th>Attribute</th>
      <th>Value</th>
      <th>Purpose</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`role`</td>
      <td>`status`</td>
      <td>Marks the spinner as a polite live region</td>
    </tr>
    <tr>
      <td>`aria-label`</td>
      <td>`"Loading"` (default) or custom</td>
      <td>Provides the accessible name announced to screen readers</td>
    </tr>
    <tr>
      <td>`aria-hidden`</td>
      <td>`"true"`</td>
      <td>On the `<svg>` element — the SVG animation is decorative</td>
    </tr>
  </tbody>
</table>

The spinner constant (`SPINNER_ROLE = 'status'`) is defined in `sando-spinner.a11y.ts` and imported by the component. The actual a11y tests live in `sando-spinner.a11y.test.ts`.

## Keyboard map

<table>
  <thead>
    <tr>
      <th>Key</th>
      <th>Behavior</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>—</td>
      <td>Not keyboard focusable — non-interactive loading indicator</td>
    </tr>
  </tbody>
</table>

## Screen reader behavior

Default: "Loading" (polite, via `role="status"`). Custom label: "Processing payment" (when `label="Processing payment"`). The SVG animation is hidden via `aria-hidden="true"` on the `<svg>`.

## Color contrast

Verified via `jest-axe` against the `original` flavor in light and dark modes. Both `default` and `inverted` variants meet WCAG 2.1 AA.

## Tested with

- jest-axe (automated, runs in `sando-spinner.a11y.test.ts`)
- VoiceOver (macOS Sequoia, Safari 18)
- NVDA (Windows 11, Firefox 130)

## Known limitations

`sando-spinner.a11y.ts` (without `.test`) is a constants file — it is not a test file. Tests are in `sando-spinner.a11y.test.ts`.

---

[Back to Accessibility Statement](?path=/docs/accessibility-statement--docs)
