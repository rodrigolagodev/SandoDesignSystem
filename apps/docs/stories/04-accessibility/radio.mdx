import { Meta } from "@storybook/blocks";

<Meta title="Accessibility/Radio" />

# Accessibility — sando-radio

> Source: [`packages/components/src/components/radio/ACCESSIBILITY.md`](https://github.com/rodrigolagodev/SandoDesignSystem/blob/master/packages/components/src/components/radio/ACCESSIBILITY.md)

## ARIA pattern

Follows the [WAI-ARIA Radio Button](https://www.w3.org/WAI/ARIA/apg/patterns/radio/) pattern via native `<input type="radio">`.

<table>
  <thead>
    <tr>
      <th>Attribute</th>
      <th>Value</th>
      <th>Purpose</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`aria-checked`</td>
      <td>`"true"` / `"false"`</td>
      <td>Communicates the checked state</td>
    </tr>
    <tr>
      <td>`aria-invalid`</td>
      <td>`"true"` / `"false"`</td>
      <td>Marks the radio as invalid when `error=true`</td>
    </tr>
    <tr>
      <td>`aria-describedby`</td>
      <td>Error/helper text ID</td>
      <td>Links error or helper text to the input</td>
    </tr>
  </tbody>
</table>

Uses a native `<input type="radio">` inside a shadow `<label>`. The visual radio box uses `role="presentation"`.

## Keyboard map

<table>
  <thead>
    <tr>
      <th>Key</th>
      <th>Behavior</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Tab</td>
      <td>Move focus to the radio group (first or selected radio)</td>
    </tr>
    <tr>
      <td>Shift+Tab</td>
      <td>Move focus backward</td>
    </tr>
    <tr>
      <td>ArrowRight / ArrowDown</td>
      <td>Select the next radio in the group</td>
    </tr>
    <tr>
      <td>ArrowLeft / ArrowUp</td>
      <td>Select the previous radio in the group</td>
    </tr>
    <tr>
      <td>Space</td>
      <td>Select the focused radio</td>
    </tr>
  </tbody>
</table>

## Screen reader behavior

Unfocused: "Option A, radio button, 1 of 3". Checked: "Option A, selected, radio button". Error: "Please select an option, invalid entry".

## Color contrast

Verified via `jest-axe` against the `original` flavor in light and dark modes. Disabled state uses intentional reduced contrast.

## Tested with

- jest-axe (automated, runs in `sando-radio.a11y.test.ts`)
- VoiceOver (macOS Sequoia, Safari 18)
- NVDA (Windows 11, Firefox 130)

## Known limitations

None identified.

---

[Back to Accessibility Statement](?path=/docs/accessibility-statement--docs)
