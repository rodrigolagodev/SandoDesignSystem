import { Meta } from "@storybook/blocks";

<Meta title="Accessibility/Radio Group" />

# Accessibility — sando-radio-group

> Source: [`packages/components/src/components/radio-group/ACCESSIBILITY.md`](https://github.com/rodrigolagodev/SandoDesignSystem/blob/master/packages/components/src/components/radio-group/ACCESSIBILITY.md)

## ARIA pattern

Follows the [WAI-ARIA Radio Group](https://www.w3.org/WAI/ARIA/apg/patterns/radio/) pattern.

<table>
  <thead>
    <tr>
      <th>Attribute</th>
      <th>Value</th>
      <th>Purpose</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`role`</td>
      <td>`radiogroup`</td>
      <td>Groups related radio buttons under a common label</td>
    </tr>
    <tr>
      <td>`aria-labelledby`</td>
      <td>Label element ID</td>
      <td>Associates the group label with the radiogroup</td>
    </tr>
    <tr>
      <td>`aria-describedby`</td>
      <td>Helper/error element ID</td>
      <td>Links helper or error text to the group</td>
    </tr>
    <tr>
      <td>`aria-required`</td>
      <td>`"true"`</td>
      <td>Marks the group as required</td>
    </tr>
    <tr>
      <td>`aria-disabled`</td>
      <td>`"true"`</td>
      <td>Marks all radios as non-interactive when group is disabled</td>
    </tr>
    <tr>
      <td>`aria-invalid`</td>
      <td>`"true"`</td>
      <td>Marks the group as invalid when `error=true`</td>
    </tr>
  </tbody>
</table>

The options container uses `role="presentation"`. Error container uses `role="alert"`.

## Keyboard map

<table>
  <thead>
    <tr>
      <th>Key</th>
      <th>Behavior</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Tab</td>
      <td>Move focus into the radio group (first or selected radio)</td>
    </tr>
    <tr>
      <td>Shift+Tab</td>
      <td>Move focus out of the radio group</td>
    </tr>
    <tr>
      <td>ArrowRight / ArrowDown</td>
      <td>Select the next radio</td>
    </tr>
    <tr>
      <td>ArrowLeft / ArrowUp</td>
      <td>Select the previous radio</td>
    </tr>
    <tr>
      <td>Space</td>
      <td>Select the focused radio</td>
    </tr>
  </tbody>
</table>

## Screen reader behavior

On focus entry: "Choose a color, group". Navigating: "Red, radio button, 1 of 3". Error: announced assertively via `role="alert"`.

## Color contrast

Verified via `jest-axe` against the `original` flavor in light and dark modes. Disabled state uses intentional reduced contrast.

## Tested with

- jest-axe (automated, runs in `sando-radio-group.a11y.test.ts`)
- VoiceOver (macOS Sequoia, Safari 18)
- NVDA (Windows 11, Firefox 130)

## Known limitations

None identified.

---

[Back to Accessibility Statement](?path=/docs/accessibility-statement--docs)
