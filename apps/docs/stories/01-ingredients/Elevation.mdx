import { Meta } from "@storybook/blocks";

<Meta title="Ingredients/Elevation" />

# Elevation

Elevation creates visual hierarchy and depth through carefully crafted shadows. The Sando Design System provides **6 elevation levels** built on **Ingredients tokens** (Layer 1).

## Elevation Scale

Our elevation system uses layered shadows to simulate depth and focus, helping users understand element hierarchy and interactive states.

<table style={{ width: "100%", borderCollapse: "collapse", marginTop: "16px" }}>
  <thead>
    <tr
      style={{
        borderBottom: "2px solid var(--sb-docs-border)",
        textAlign: "left",
      }}
    >
      <th style={{ padding: "12px 16px", fontWeight: "600" }}>Token</th>
      <th style={{ padding: "12px 16px", fontWeight: "600" }}>Preview</th>
      <th style={{ padding: "12px 16px", fontWeight: "600" }}>Name</th>
      <th style={{ padding: "12px 16px", fontWeight: "600" }}>Description</th>
    </tr>
  </thead>
  <tbody>
    {[
      { token: "0", name: "None", description: "Flat, no depth" },
      {
        token: "100",
        name: "Subtle",
        description: "Slight lift, hover states",
      },
      { token: "200", name: "Low", description: "Cards, buttons" },
      { token: "300", name: "Medium", description: "Dropdowns, popovers" },
      { token: "400", name: "High", description: "Modals, dialogs" },
      {
        token: "500",
        name: "Highest",
        description: "Tooltips, top-level overlays",
      },
    ].map((item, index) => (
      <tr
        key={item.token}
        style={{
          borderBottom: "1px solid var(--sb-docs-border)",
          backgroundColor:
            index % 2 === 1 ? "var(--sb-docs-bg-emphasis)" : "transparent",
        }}
      >
        <td style={{ padding: "12px 16px" }}>
          <code>elevation-{item.token}</code>
        </td>
        <td style={{ padding: "12px 16px" }}>
          <div
            style={{
              width: "100px",
              height: "60px",
              backgroundColor: "var(--sb-docs-bg-emphasis)",
              border: "1px solid var(--sb-docs-border)",
              borderRadius: "8px",
              boxShadow: `var(--sando-elevation-${item.token})`,
              display: "flex",
              alignItems: "center",
              justifyContent: "center",
              fontSize: "12px",
              color: "var(--sb-docs-text-caption)",
            }}
          >
            {item.name}
          </div>
        </td>
        <td style={{ padding: "12px 16px", fontWeight: "600" }}>{item.name}</td>
        <td
          style={{ padding: "12px 16px", color: "var(--sb-docs-text-muted)" }}
        >
          {item.description}
        </td>
      </tr>
    ))}
  </tbody>
</table>

---

## Elevation Hierarchy

Elevation creates a visual z-axis, with higher elevations appearing "closer" to the user.

<div
  style={{
    marginTop: "32px",
    position: "relative",
    height: "400px",
    backgroundColor: "var(--sb-docs-bg-base)",
    borderRadius: "12px",
    padding: "40px",
    overflow: "hidden",
  }}
>
  <div
    style={{
      position: "absolute",
      bottom: "40px",
      left: "40px",
      backgroundColor: "var(--sb-docs-bg-emphasis)",
      border: "1px solid var(--sb-docs-border)",
      padding: "24px",
      borderRadius: "8px",
      boxShadow: "var(--sando-elevation-100)",
      zIndex: 1,
    }}
  >
    <div style={{ fontSize: "14px", fontWeight: "600", color: "var(--sb-docs-text-heading)" }}>
      elevation-100
    </div>
    <div style={{ fontSize: "12px", color: "var(--sb-docs-text-caption)" }}>Base layer</div>
  </div>

<div
  style={{
    position: "absolute",
    bottom: "80px",
    left: "120px",
    backgroundColor: "var(--sb-docs-bg-emphasis)",
    border: "1px solid var(--sb-docs-border)",
    padding: "24px",
    borderRadius: "8px",
    boxShadow: "var(--sando-elevation-200)",
    zIndex: 2,
  }}
>
  <div
    style={{
      fontSize: "14px",
      fontWeight: "600",
      color: "var(--sb-docs-text-heading)",
    }}
  >
    elevation-200
  </div>
  <div style={{ fontSize: "12px", color: "var(--sb-docs-text-caption)" }}>
    Raised cards
  </div>
</div>

<div
  style={{
    position: "absolute",
    bottom: "120px",
    left: "200px",
    backgroundColor: "var(--sb-docs-bg-emphasis)",
    border: "1px solid var(--sb-docs-border)",
    padding: "24px",
    borderRadius: "8px",
    boxShadow: "var(--sando-elevation-300)",
    zIndex: 3,
  }}
>
  <div
    style={{
      fontSize: "14px",
      fontWeight: "600",
      color: "var(--sb-docs-text-heading)",
    }}
  >
    elevation-300
  </div>
  <div style={{ fontSize: "12px", color: "var(--sb-docs-text-caption)" }}>
    Dropdowns
  </div>
</div>

<div
  style={{
    position: "absolute",
    bottom: "160px",
    left: "280px",
    backgroundColor: "var(--sb-docs-bg-emphasis)",
    border: "1px solid var(--sb-docs-border)",
    padding: "24px",
    borderRadius: "8px",
    boxShadow: "var(--sando-elevation-400)",
    zIndex: 4,
  }}
>
  <div
    style={{
      fontSize: "14px",
      fontWeight: "600",
      color: "var(--sb-docs-text-heading)",
    }}
  >
    elevation-400
  </div>
  <div style={{ fontSize: "12px", color: "var(--sb-docs-text-caption)" }}>
    Modals
  </div>
</div>

  <div
    style={{
      position: "absolute",
      bottom: "200px",
      left: "360px",
      backgroundColor: "var(--sb-docs-bg-emphasis)",
      border: "1px solid var(--sb-docs-border)",
      padding: "24px",
      borderRadius: "8px",
      boxShadow: "var(--sando-elevation-500)",
      zIndex: 5,
    }}
  >
    <div style={{ fontSize: "14px", fontWeight: "600", color: "var(--sb-docs-text-heading)" }}>
      elevation-500
    </div>
    <div style={{ fontSize: "12px", color: "var(--sb-docs-text-caption)" }}>Tooltips</div>
  </div>
</div>

---

## Component Examples

### Cards with Elevation

<div
  style={{ marginTop: "32px", display: "flex", gap: "24px", flexWrap: "wrap" }}
>
  <div
    style={{
      flex: "1 1 250px",
      backgroundColor: "var(--sb-docs-bg-emphasis)",
      border: "1px solid var(--sb-docs-border)",
      padding: "24px",
      borderRadius: "12px",
      boxShadow: "var(--sando-elevation-0)",
    }}
  >
    <div
      style={{
        fontSize: "16px",
        fontWeight: "600",
        marginBottom: "8px",
        color: "var(--sb-docs-text-heading)",
      }}
    >
      Flat Card
    </div>
    <div style={{ fontSize: "14px", color: "var(--sb-docs-text-body)" }}>
      No elevation, sits flush with background
    </div>
  </div>

<div
  style={{
    flex: "1 1 250px",
    backgroundColor: "var(--sb-docs-bg-emphasis)",
    border: "1px solid var(--sb-docs-border)",
    padding: "24px",
    borderRadius: "12px",
    boxShadow: "var(--sando-elevation-100)",
  }}
>
  <div
    style={{
      fontSize: "16px",
      fontWeight: "600",
      marginBottom: "8px",
      color: "var(--sb-docs-text-heading)",
    }}
  >
    Subtle Card
  </div>
  <div style={{ fontSize: "14px", color: "var(--sb-docs-text-body)" }}>
    Minimal shadow for subtle separation
  </div>
</div>

  <div
    style={{
      flex: "1 1 250px",
      backgroundColor: "var(--sb-docs-bg-emphasis)",
      border: "1px solid var(--sb-docs-border)",
      padding: "24px",
      borderRadius: "12px",
      boxShadow: "var(--sando-elevation-200)",
    }}
  >
    <div
      style={{
        fontSize: "16px",
        fontWeight: "600",
        marginBottom: "8px",
        color: "var(--sb-docs-text-heading)",
      }}
    >
      Raised Card
    </div>
    <div style={{ fontSize: "14px", color: "var(--sb-docs-text-body)" }}>
      Clear depth, draws attention
    </div>
  </div>
</div>

### Interactive States

Elevation can change on interaction to provide feedback.

<div
  style={{ marginTop: "32px", display: "flex", gap: "24px", flexWrap: "wrap" }}
>
  <div
    style={{
      flex: "1 1 250px",
      backgroundColor: "var(--sb-docs-link)",
      color: "white",
      padding: "16px 32px",
      borderRadius: "8px",
      boxShadow: "var(--sando-elevation-100)",
      textAlign: "center",
      fontSize: "14px",
      fontWeight: "600",
      cursor: "pointer",
      transition: "box-shadow 0.2s ease, transform 0.2s ease",
    }}
    onMouseEnter={(e) => {
      e.currentTarget.style.boxShadow = "var(--sando-elevation-200)";
      e.currentTarget.style.transform = "translateY(-2px)";
    }}
    onMouseLeave={(e) => {
      e.currentTarget.style.boxShadow = "var(--sando-elevation-100)";
      e.currentTarget.style.transform = "translateY(0)";
    }}
  >
    Hover me
  </div>

  <div
    style={{
      flex: "1 1 250px",
      backgroundColor: "var(--sb-docs-bg-emphasis)",
      border: "1px solid var(--sb-docs-border)",
      padding: "24px",
      borderRadius: "12px",
      boxShadow: "var(--sando-elevation-200)",
      textAlign: "center",
      fontSize: "14px",
      fontWeight: "600",
      color: "var(--sb-docs-text-heading)",
      cursor: "pointer",
      transition: "box-shadow 0.2s ease, transform 0.2s ease",
    }}
    onMouseEnter={(e) => {
      e.currentTarget.style.boxShadow = "var(--sando-elevation-300)";
      e.currentTarget.style.transform = "translateY(-4px)";
    }}
    onMouseLeave={(e) => {
      e.currentTarget.style.boxShadow = "var(--sando-elevation-200)";
      e.currentTarget.style.transform = "translateY(0)";
    }}
  >
    Card with hover lift
  </div>
</div>

---

## Usage Guidelines

### When to Use Elevation

- **elevation-0**: Flat elements, embedded content
- **elevation-100**: Subtle separation, hover states for flat elements
- **elevation-200**: Cards, buttons, raised content
- **elevation-300**: Dropdowns, popovers, floating menus
- **elevation-400**: Modal dialogs, sheets, overlays
- **elevation-500**: Tooltips, toast notifications, top-level UI

### Do's

- ✅ Use elevation to establish visual hierarchy
- ✅ Increase elevation on hover for interactive elements
- ✅ Keep elevation consistent for similar components
- ✅ Use higher elevations sparingly for critical UI

### Don'ts

- ❌ Don't use too many elevation levels in one view
- ❌ Don't use elevation without semantic meaning
- ❌ Don't mix elevation systems
- ❌ Don't use elevation as decoration only

---

## Dark Mode Behavior

Elevation shadows use `rgba(0,0,0,...)` values optimized for light backgrounds. In dark mode, elevation is communicated through:

- **Surface lightening** — elevated elements use lighter surface colors to stand out from the base background
- **Subtle borders** — provide boundary definition when shadows lack contrast against dark backgrounds

This follows the established Material Design pattern where dark mode elevation relies on surface luminance rather than shadow depth. The Ingredient tokens remain unchanged — dark mode adaptation happens at the documentation level through appropriate background choices.

---

## Accessibility

- Elevation alone should not convey critical information
- Combine with color, text, or icons for important states
- Ensure sufficient contrast for shadow visibility
- Consider motion-reduced preferences for elevation transitions

---

## Related

- **Flavors Layer** - Semantic elevation tokens (muted, default, emphasis)
- **Components** - Elevation in context
- **Animation** - Elevation transition timing
