import { Meta } from "@storybook/blocks";

<Meta title="Welcome" />

<style>
  {`
    .sando-welcome {
      max-width: 880px;
      margin: 0 auto;
      padding: var(--sando-space-12, 4rem) var(--sando-space-6, 1.5rem);
      font-family: var(--sando-font-family-body, system-ui, sans-serif);
      color: var(--sando-color-text-body, #1e1a15);
    }

    .sando-welcome__brand {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: var(--sando-space-6, 1.5rem);
      text-align: center;
    }

    .sando-welcome__logo {
      width: 100%;
      max-width: 280px;
      height: auto;
    }

    html[data-color-mode="dark"] .sando-welcome__logo {
      filter: invert(1) hue-rotate(180deg);
    }

    .sando-welcome__tagline {
      max-width: 540px;
      margin: 0;
      font-size: 1.125rem;
      line-height: 1.6;
      color: var(--sando-color-text-caption, #5f5a54);
    }

    .sando-welcome__cta-row {
      display: flex;
      flex-wrap: wrap;
      gap: var(--sando-space-3, 0.75rem);
      justify-content: center;
      margin-top: var(--sando-space-4, 1rem);
    }

    .sando-welcome__cta {
      display: inline-flex;
      align-items: center;
      padding: 0.625rem 1.25rem;
      border-radius: var(--sando-border-radius-default, 6px);
      font-family: inherit;
      font-size: 0.9375rem;
      font-weight: 500;
      text-decoration: none;
      transition: background-color 0.15s ease, border-color 0.15s ease, color 0.15s ease;
    }

    .sando-welcome__cta--primary {
      background: var(--sando-color-action-solid-background-default, #b45600);
      color: var(--sando-color-action-solid-text-default, #ffffff);
    }

    .sando-welcome__cta--primary:hover {
      background: var(--sando-color-action-solid-background-hover, #973b00);
    }

    .sando-welcome__cta--secondary {
      background: transparent;
      color: var(--sando-color-action-outline-text-default, #1e1a15);
      border: 1px solid var(--sando-color-border-default, #cbc3b9);
    }

    .sando-welcome__cta--secondary:hover {
      border-color: var(--sando-color-action-outline-border-hover, #b45600);
      color: var(--sando-color-action-outline-border-hover, #b45600);
    }

    .sando-welcome__divider {
      margin: var(--sando-space-12, 4rem) 0 var(--sando-space-8, 2rem);
      border: 0;
      border-top: 1px solid var(--sando-color-border-muted, #e6dcd2);
    }

    .sando-welcome__layers {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: var(--sando-space-4, 1rem);
    }

    .sando-welcome__layer {
      padding: var(--sando-space-5, 1.25rem) var(--sando-space-5, 1.25rem);
      border: 1px solid var(--sando-color-border-muted, #e6dcd2);
      border-radius: var(--sando-border-radius-default, 6px);
      background: var(--sando-color-background-surface, transparent);
    }

    .sando-welcome__layer-eyebrow {
      display: block;
      margin-bottom: var(--sando-space-2, 0.5rem);
      font-family: var(--sando-font-family-mono, monospace);
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.06em;
      color: var(--sando-color-text-muted, #928b83);
    }

    .sando-welcome__layer-title {
      margin: 0 0 var(--sando-space-2, 0.5rem);
      font-family: var(--sando-font-family-heading, var(--sando-font-family-body, system-ui));
      font-size: 1.125rem;
      font-weight: 600;
      color: var(--sando-color-text-heading, #1e1a15);
    }

    .sando-welcome__layer-text {
      margin: 0;
      font-size: 0.9375rem;
      line-height: 1.55;
      color: var(--sando-color-text-body, #47413c);
    }

    .sando-welcome__footer {
      margin-top: var(--sando-space-10, 3rem);
      text-align: center;
      font-size: 0.875rem;
      color: var(--sando-color-text-muted, #928b83);
    }

    .sando-welcome__footer a {
      color: inherit;
      text-decoration: underline;
      text-underline-offset: 3px;
    }

    .sando-welcome__footer a:hover {
      color: var(--sando-color-text-link, #b45600);
    }
  `}
</style>

<div className="sando-welcome">

  <div className="sando-welcome__brand">
    <img
      className="sando-welcome__logo"
      src="./sando-logo.svg"
      alt="Sando Design System"
    />
    <p className="sando-welcome__tagline">
      A modern, accessible, token-based design system built on Web Components.
      Three layers of design tokens — Ingredients, Flavors, Recipes — keep
      theming predictable and components consistent.
    </p>
    <div className="sando-welcome__cta-row">
      <a
        className="sando-welcome__cta sando-welcome__cta--primary"
        href="?path=/docs/getting-started-introduction--docs"
      >
        Get started
      </a>
      <a
        className="sando-welcome__cta sando-welcome__cta--secondary"
        href="?path=/docs/components-status--docs"
      >
        Components
      </a>
      <a
        className="sando-welcome__cta sando-welcome__cta--secondary"
        href="https://github.com/rodrigolagodev/SandoDesignSystem"
        target="_blank"
        rel="noopener noreferrer"
      >
        GitHub →
      </a>
    </div>
  </div>

  <hr className="sando-welcome__divider" />

  <div className="sando-welcome__layers">
    <div className="sando-welcome__layer">
      <span className="sando-welcome__layer-eyebrow">Layer 1</span>
      <h3 className="sando-welcome__layer-title">Ingredients</h3>
      <p className="sando-welcome__layer-text">
        Raw primitives — color scales, spacing, typography, motion. The pantry.
      </p>
    </div>
    <div className="sando-welcome__layer">
      <span className="sando-welcome__layer-eyebrow">Layer 2</span>
      <h3 className="sando-welcome__layer-title">Flavors</h3>
      <p className="sando-welcome__layer-text">
        Semantic roles — action, surface, text, border. The seasoning that
        defines a theme.
      </p>
    </div>
    <div className="sando-welcome__layer">
      <span className="sando-welcome__layer-eyebrow">Layer 3</span>
      <h3 className="sando-welcome__layer-title">Recipes</h3>
      <p className="sando-welcome__layer-text">
        Component-scoped tokens — button, input, checkbox. The plated dish.
      </p>
    </div>
  </div>

  <p className="sando-welcome__footer">
    Try the <strong>Flavor</strong> and <strong>Mode</strong> switchers in the
    toolbar above to see the system adapt in real time.
  </p>

</div>
