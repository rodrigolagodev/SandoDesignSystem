/**
 * Sando Badge Component
 *
 * A purely informative label component for displaying states, categories,
 * and quick labels. Non-interactive (no click handlers, no hover states).
 *
 * @module badge
 */

export { SandoBadge } from './sando-badge.js';
export type {
  BadgeColor,
  BadgeVariant,
  BadgeSize,
  BadgeSemanticColor,
  SandoBadgeProps
} from './sando-badge.types.js';
export { BADGE_SEMANTIC_ICONS } from './sando-badge.types.js';
