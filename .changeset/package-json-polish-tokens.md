---
"@sando-ds/tokens": patch
---

Add `sideEffects` allow-list for CSS files, drop `src/` from the published tarball, add `LICENSE` alongside the package, and add `engines.node >=20`, `repository`, `homepage`, and `bugs` metadata. No API changes.
