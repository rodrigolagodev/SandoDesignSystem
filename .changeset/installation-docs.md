---
"@sando-ds/components": patch
"@sando-ds/tokens": patch
---

Added a comprehensive Installation guide covering four token import strategies (full bundle, per-layer, per-component, per-flavor) so consumers can pick the right granularity for their bundle-size budget.
