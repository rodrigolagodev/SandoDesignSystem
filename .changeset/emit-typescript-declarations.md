---
"@sando/components": patch
---

Package now ships TypeScript declarations (`.d.ts` + `.d.ts.map`) for all entry points. Consumers no longer get `Could not find a declaration file for module '@sando/components'` errors and get full IntelliSense plus go-to-definition into the original sources.
